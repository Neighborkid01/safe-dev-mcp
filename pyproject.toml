[project]
name = "safe-dev-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp>=2,<3"]

[tool.ruff]
target-version = "py311"

[tool.pyright]
venvPath = "."
venv = ".venv"
typeCheckingMode = "standard"
